import FightFigure from "./FightFigure";

export default class FightEnemy extends FightFigure {
    constructor(loader, x, y, container, area, type) {
        super(loader, x, y, container, area, type);
    }
}